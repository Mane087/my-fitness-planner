## Contexto:

Se detalla que se fue lo que hizo (maximo 500 caracteres)

## ¿Qué cambia?

Se detalla que es lo que se cambio (formato de listado) **NOTA:** **NO ES NECESARIO HACER MENCION DE LOS ARCHIVOS, EL PR YA LOS ADJUNTA EN LOS CAMBIOS**
Ejemplo:

- Se agrego un nuevo endpoint para obtener los usuarios

## ¿Por qué cambia?

Se detalla el por que del cambio (maximo 500 caracteres)
Ejemplo:
Se agrego el endpoint para la consulta de usuarios porque la API de usuarios no devolvia los datos necesarios para la aplicacion.

## ¿Cómo probarlo?

Se detalla como se puede probar el cambio (formato de listado)
Ejemplo:

- Entrar a la ruta /usuarios
- Consultar el endpoint /usuarios
- Verificar que se muestren los datos correctamente

## Evidencia (Opcional)

Se agregan capturas, codigo json o alguna otra evidencia que demuestre que los cambios aplicados funcionan

## Closes

Se detalla que issues se cierran con este PR (formato de listado)
Ejemplo:

- #123
- #456
- #789
